apiVersion: v1
kind: Service
metadata:
  name: scaletestapp-svc
  namespace: sprint8
  labels:
    app: scaletestapp
spec:
  selector:
    app: scaletestapp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer